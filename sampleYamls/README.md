# What is this directory ?
Contains sample codes related to kubernetes, mostly related to Kind files, eg. deployment, configmaps, secrets etc

# How to run?
```
$ minikube start
$ kubectl apply -f <insert_file_name_here>
```